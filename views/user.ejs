<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Details</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 20px;
            background-color: #f5f5f5;
        }

        h1 {
            color: #333;
            text-align: center;
        }

        .container-inner-card {
            display: flex;
            justify-content: space-between;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin: 20px auto;
            max-width: 800px;
        }

        .user-details {
            max-width: 400px;
        }

        h3 {
            color: #007BFF;
        }

        p {
            color: #555;
        }

        .created-at,
        .updated-at {
            margin-top: 10px;
            color: #888;
        }

        .updated-list {
            max-height: 100px; /* Set your desired max-height */
            overflow-y: auto;
            padding-left: 20px; /* Add some left padding for better readability */
        }

        img {
            max-width: 20rem;
            height: auto;
            border-radius: 8px;
        }

        a {
            display: inline-block;
            margin-top: 10px;
            padding: 10px 20px;
            background-color: #007BFF;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        a:hover {
            background-color: #0056b3;
        }

        .back-link {
            background-color: #6c757d;
            margin-right: 10px;
        }

        .back-link:hover {
            background-color: #495057;
        }
    </style>
</head>

<body>
    <h1>User Details</h1>

    <div class="container-inner-card">
        <div class="user-details">
            <h3>Name: <%= user.name %></h3>
            <p>Age: <%= user.age %></p>
            <p>City: <%= user.city %></p>
            <p>Nationality: <%= user.nationality %></p>
            <p class="created-at">Created at: <%= user.created_at %></p>
            <p class="updated-at">
                <% if (user.updated_at && user.updated_at.length > 0) { %>
                    Updated at:
                    <ul class="updated-list">
                        <% user.updated_at.forEach((updatedAt) => { %>
                            <li><%= updatedAt %></li>
                        <% }); %>
                    </ul>
                <% } else { %>
                    No updates yet.
                <% } %>
            </p>
        </div>
        <div>
            <img src="http://localhost:8000/<%= user.img_path %>" alt="User Image">
        </div>
    </div>

    <a href="/editUser/<%= user.user_id %>" class="edit-link">Edit User</a>
    <a href="/" class="back-link">Back to All Users</a>
</body>

</html>
